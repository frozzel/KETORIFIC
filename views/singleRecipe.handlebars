 <article class="card">
 <div class="card-header flex-row justify-space-between" id="singleRecipe">
      <img src="{{recipe.image}}"  class="card-img-top" alt="...">
      <h2>{{recipe.title}}</h2>
       <span><h3>{{recipe.ingredients}}</h3></span>
       <span><p class="card-text">{{recipe.ingredients}}</p></span>
    </div>
  </article>